/* =====================================================================
   Summer of SQL — Week XX — Challenge NN
   ---------------------------------------------------------------------
   Title      : <challenge title>
   Link       : <url>
   Date       : YYYY-MM-DD
   Tests      : <e.g. window functions, joins, aggregation>
   Dialect    : Snowflake
   ===================================================================== */

-- Approach:
-- <one or two lines on how you solved it>

SELECT
    ...
FROM ...
;
